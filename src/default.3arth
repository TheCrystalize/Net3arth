// command search: F1
// Tutorial: Ctrl-? or Cmd-? (without shift)

// List of commands:
// Ctrl--Shift-Space or Cmd-Shift-Space

const scaleFactor = 0.7;

transform square(number a):
  scale(scaleFactor)
  -> rotate(a*45)
  -> translate(a*-0.5, -1);

body:
hslShift(0.1, 0, 0)
-> choose{
  1: blurSquare() -> color(colorHSL(0, 1, 0.5));
  3: square(-1);
  3: square( 1);
};

camera:
translate(0, 1)
-> scale(0.15);

shader: gamma(2);
