transform halfway(number x, number y):
  scale(1/2) -> translate(x/2, y/2);

body:
hslShift(0.1, 0, 0)
-> choose{
  1: halfway( 0,-1);
  1: halfway(-1, 1);
  1: halfway( 1, 1);
};


/*
tutorial:
points that go halfway

on the left, a circle
go halfway to the right

add a second one to demonstrate a line

add a 3rd one to demonstrate a sirpinski triangle
 */


 body:
 blurCircle()
 -> sum {
   1: scale(1);
   -1: circleInv();
 };

 camera:
 scale(1/2);
